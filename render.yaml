# render.yaml

services:
  - name: your-fastify-app
    type: web
    buildCommand: npm install
    startCommand: fastify start -l info app.js
    envVars:
      - key: NODE_ENV
        value: production
